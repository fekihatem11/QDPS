# `qdps/features_for_selection/`

VGG16 image features used by **QDPS / SETS test-selection algorithms** when
they compute the diversity kernel.

> Not used by the fault-clustering pipeline — that needs *raw* VGG features
> in a different normalization. Those live in
> `qdps/robustness/features_for_clustering/`.

---

